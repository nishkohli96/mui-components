# Changelog - v2

## [2.1.0](https://github.com/nishkohli96/mui-components/tree/v2.1.0)

### ✨ Enhancements
- Added `applicationName`, `authors` and `creator` to the root page metadata.
- `PageHeading`/`SubHeading`: render as real `<h1>`/`<h2>` elements (`component="h1"`/`"h2"`) instead of just `<h4>`/`<h6>`-styled `<div>`s, giving pages a correct heading hierarchy.
- `sitemap.ts`: added `changeFrequency: 'monthly'` to every entry.

### 📖 Docs
- `componentMetadata`: titles for `MUITextField`, `MUIDatePicker`, `MUITimePicker` and `MUIDateTimePicker` now use the component name (e.g. `MUITextField`) instead of a generic label (e.g. `TextField`, `Date Pickers`), matching every other component's title and browser-tab text.
- Heading anchor links (`#`) are now drawn via CSS (`::before`) instead of a text node, so the `#` no longer shows up in copied text, screen-reader output, or search snippets; marked `aria-hidden` since the page TOC already covers real navigation.

### 🐞 Bug Fixes
- `CodeSnippets`' copy-button reset delay bumped from 1500ms to 2000ms, matching `MdxPre`'s copy button.
- Homepage: reverted the "Material UI" link's underline back to `hover`-only.


## [2.0.4](https://github.com/nishkohli96/mui-components/tree/v2.0.4)

**Released - 28 August, 2026**

### 📖 Docs
- Added `app/sitemap.ts` and `app/robots.ts`, generated from `sidebarLinks` (current docs version only) so they can't drift from the real site nav.
- Added a self-referencing `alternates.canonical` to every current-version page.
- Added `robots: { index: false, follow: true }` to the `/v1` layout, so every `v1` page is excluded from search results instead of competing with its `v2` twin.
- Set `metadataBase` and default `openGraph`/`twitter` metadata in the root layout, including an auto-generated `opengraph-image.tsx` (reuses the homepage wordmark gradient) — every page now gets a proper social-preview card instead of a bare-text share.
- Removed the `keywords` meta tag (`defaultPageKeywords`) — ignored by Google since 2009, and leaked the target-keyword list to anyone viewing page source.
- Added structured data: `Person` (site author) in the root layout, `BreadcrumbList` on every doc page, and `SoftwareSourceCode` (pointing at the component's actual source file) on every component page — all via `PageNav`, reusing its existing `pathname`/breadcrumb-trail logic.
- Added `/llms.txt`, generated from `sidebarLinks` the same way as the sitemap.
- Added a `Cache-Control` header (`public, s-maxage=300, stale-while-revalidate=86400`) for all doc routes in `next.config.ts`, replacing Vercel's static-HTML default of `public, max-age=0, must-revalidate`.

### 🐞 Bug Fixes
- Sidebar: removed `unmountOnExit` from the mobile/desktop nav `Collapse` — collapsed sections were dropping their links entirely from the server-rendered HTML, leaving every page outside the one auto-expanded section unreachable without JS.
- `MUIFileUploader`: Fixed single-file mode wiping the current value to `null` when a replacement file was rejected (invalid type/size or over `maxFiles`) — the current file is now kept instead.


## [2.0.3](https://github.com/nishkohli96/mui-components/tree/v2.0.3)

**Released - 13 August, 2026**

### ✨ Enhancements
- Added JSDoc summaries (with `Docs`/`API` links) to every `MUI*` component, `DefaultMUIComponentsConfig`, `ConfigProvider`, and `countryList`.

### 📖 Docs
- Renamed the **Props** section to **API** on every component page, so the reference table anchor matches the new `#api` JSDoc links.
- Blur the version-switcher `Select` after choosing a docs version, instead of leaving it focused.
- Swapped the sidebar order of `getFileSize` and `validateFileList` to match their declaration order in `form-helpers`.

### 🐞 Bug Fixes
- `MUIPhoneInput`: The country search field stopped propagation on every keydown, preventing "**Escape**" key from reaching MUI's `Menu` and closing the dropdown; Escape now propagates normally while other keys are still blocked from reaching `Select`.


## [2.0.2](https://github.com/nishkohli96/mui-components/tree/v2.0.2)

**Released - 12 August, 2026**

### ⚙️ Project & Tooling
- Added a `prod` script (`next build && next start`) to `apps/docs`, runnable from the root as `pnpm docs:prod`.

### ✨ Enhancements
- Added JSDoc comments for `fieldNameToId`, `getFileSize` and `validateFileList` functions, exported from the `form-helpers` module.

### 🐞 Bug Fixes
- `getFileSize`: Fixed trailing zeros (e.g. `1.20`) not being stripped from the decimal part.
- `validateFileList`: Fixed `maxSize={0}` and `maxFiles={0}` being treated as "no limit" instead of rejecting all files.


## [2.0.1](https://github.com/nishkohli96/mui-components/tree/v2.0.1)

**Released - 8 August, 2026**

### 📖 Docs
- Commented out the embedded StackBlitz playground links on header, `README.md` and `v1` component pages — StackBlitz's GitHub-import can't reliably clone/build this repo (broken `pnpm-workspace.yaml`, then a Turbopack/WASM binding failure even after forcing `--webpack`). Until a working live-edit alternative is set up, the **Playground Section** in `README.md` points to the `v2-playground` or `v1-playground` branch.
- Update content of `v1` to `v2` migration guide. 
- Update `v1` docs for components with newly added props in `v2`.
- Add `margin-left` of **8px** between logo and wordmark on drawer.

### 🐞 Bug Fixes
- Handle `pokeApi` call in `try-catch` block.
- `MUIPhoneInput`
  - Fix country dropdown scrollbar spanning full menu height alongside sticky search field; scroll region now starts below it.
  - Removed `multiple` and `multiline` from `countrySelectProps`; both are always `false`.
  - Fixed a console warning ("out-of-range value") and a brief mispositioned flash when selecting a country while the search filter was active — every country now stays mounted as a `MenuItem` (hidden via CSS instead of removed) so the selected value always has a matching option.

## [2.0.0](https://github.com/nishkohli96/mui-components/tree/v2.0.0)

**Released - 7 August, 2026**

### ⚙️ Project & Tooling
- Upgraded `pnpm` from `11.10.0` to `11.20.0`.
- Added links to the corresponding StackBlitz playground for each `v1` component.

### 💥 Breaking Changes
- Upgraded to **Material UI v9** and **MUI X Date Pickers v9**. `peerDependencies` are now `^9.0.0` for `@mui/material`, `@mui/system`, `@mui/icons-material` and `@mui/x-date-pickers` — the built output calls `v9`-only APIs, so MUI `v6`/`v7` are no longer supported.

  No prop on any `MUI*` component was renamed or removed, but props forwarded *through* them into MUI (`textFieldProps`, `checkboxProps`, `slotProps.textField`) follow MUI's new slot shapes.
- Preserved floating-label shrink behaviour on every Autocomplete. In `v7` the label props reached the `TextField` implicitly through the params rest-spread; in `v9` they moved inside `slotProps.inputLabel`, so the rest-spread silently stopped delivering them and labels overlapped the selected value.
- Preserved picker accessibility attributes (`aria-labelledby`, `aria-describedby`, `aria-invalid`, `aria-required`), which would otherwise have been dropped silently since `v9` ignores the old `inputProps` on picker text fields.
- Docs: replaced the `*Outline` icons removed in `v9` with their `*Outlined` counterparts.
- Docs: reworked the dark-mode `MuiAlert` theme override for `v9`, which collapsed the per-severity `standard{Info,Success,Warning,Error}` style-override slots into a single `standard` slot driven by `ownerState.severity`.

### ✨ Enhancements
- Migrated the Autocomplete family (`MUIAutocomplete`, `MUIAutocompleteObject`, `MUIMultiAutocomplete`, `MUIMultiAutocompleteObject`, `MUICountrySelect`) to v9's reshaped `AutocompleteRenderInputParams`, where `InputProps`/`inputProps` were collapsed into `slotProps.{input,htmlInput,inputLabel}`.
- Migrated all nine date/time pickers from the removed `slotProps.textField.inputProps` to `slotProps.textField.slotProps.htmlInput`.
- Migrated `MUIPhoneInput`'s country menu from the removed `MenuProps.PaperProps`/`MenuProps.MenuListProps` to `MenuProps.slotProps.paper`/`MenuProps.slotProps.list`.
- Replaced removed MUI system props (`marginRight`, `color`, `textAlign`, `alignItems`, etc.) with their `sx` equivalents.
- Exposed previously-unreachable internal sub-component props:
  - `MUIPasswordInput`: `iconButtonProps` for the show/hide toggle button.
  - `MUISelect`: `menuItemProps` (every option `MenuItem`) and `inputLabelProps` (the inline `InputLabel`).
  - `MUIAutocomplete`, `MUIAutocompleteObject`, `MUIMultiAutocomplete`, `MUIMultiAutocompleteObject`: `circularProgressProps` for the loading spinner.
  - `MUIPhoneInput`: `countrySelectProps` for the flag/dial-code trigger `Select`, and `searchCountryProps.menuItemProps` for every country `MenuItem`.
- Reorganized component props in the props table based on their relevance.
- Updated form examples with newly added props

### 🐞 Bug Fixes
- `MUINumberInput`: Set `slotProps.htmlInput.step` to `any` (except for `onlyIntegers`) for decimal inputs (was reusing `resolvedStepAmount`), preventing false "stepMismatch" validation errors on values like 23.45.
- Docs: Correct return value of `MUICountrySelect` in the **Usage Section**.
- Docs: Corrected the `value prop` description for `RHFCheckboxGroup` by removing `null`.
- Docs: Set **Paper** `elevation` to `0` for `Drawer` for consistent appearance, esp in dark mode.
