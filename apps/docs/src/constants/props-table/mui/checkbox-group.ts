import type { PropsInfo } from '@/types';
import { PropsDescription as P } from '../descriptions';

/** Props reference rows for `MUICheckboxGroup`. */
const checkboxGroupRows: PropsInfo[] = [
  P.fieldName,
  P.value_CheckboxGroup,
  P.onValueChange_CheckboxGroup,
  P.options,
  P.labelKey,
  P.valueKey,
  P.renderOptionLabel,
  P.getOptionDisabled,
  P.checkboxProps,
  P.formControlLabelProps,
  P.required,
  P.errorMessage,
  P.renderError,
  P.hideErrorMessage,
  P.helperText,
  P.showLabelAboveFormField_Default,
  P.hideLabel,
  P.formLabelProps,
  P.formHelperTextProps,
  P.customIds
];

export default checkboxGroupRows;
