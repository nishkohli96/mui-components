import { DocsPage, PropsTable } from '@/components/docs';
import { componentMetadata, componentPropsV1,
  stackblitzFileLink } from '@/constants';

export const metadata = componentMetadata.MUIMultiAutocompleteObject;

export default function Layout({ children }) {
  return <DocsPage>{children}</DocsPage>;
}

# MUIMultiAutocompleteObject

`MUIMultiAutocompleteObject` is a multi-select [Autocomplete](https://v7.mui.com/material-ui/react-autocomplete/) with checkboxes and
an optional "**Select All**" row that stores an array of **complete option objects**.

## Usage

```tsx
import MUIMultiAutocompleteObject, {
  MUIMultiAutocompleteObjectProps
} from '@nish1896/mui-components/mui/multi-autocomplete-object';
```

```tsx
const [members, setMembers] = useState<User[]>([]);

<MUIMultiAutocompleteObject
  fieldName="members"
  value={members}
  onValueChange={({ newValue }) => setMembers(newValue)}
  options={users}
  labelKey="name"
  valueKey="id"
/>
```

## Props

`labelKey` and `valueKey` are required. Props marked with <strong style={{ color: 'var(--mui-palette-error-main)' }}>{'*'}</strong> are required.

<PropsTable rows={componentPropsV1.MUIMultiAutocompleteObject} />

## Playground

Driven by plain React state — a checkbox multi-select storing whole objects,
with a Select-All option, `limitTags`, `ChipProps` and a custom `renderOptionLabel`.

Playgrounds for older versions are hosted on StackBlitz and are not embedded inline in the component documentation.

<a href={stackblitzFileLink('src/forms/mui/multi-autocomplete-object/index.tsx')} target="_blank" rel="noopener noreferrer">
  <img src="https://developer.stackblitz.com/img/open_in_stackblitz.svg" alt="Open in StackBlitz" />
</a>
