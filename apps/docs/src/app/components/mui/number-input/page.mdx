import { DocsPage, PropsTable } from '@/components/docs';
import { ComponentProps } from '@/constants/props-table';

export const metadata = {
  title: 'MUINumberInput',
  description: 'Controlled numeric Material UI input with decimal, integer, non-negative and step constraints.'
};

export default function Layout({ children }) {
  return <DocsPage>{children}</DocsPage>;
}

# MUINumberInput

`MUINumberInput` is a [TextField](https://v7.mui.com/material-ui/react-text-field/)-based numeric input that enforces number-only entry with options for integers-only, non-negative values, a maximum number of decimal places, and keyboard step increments.

## Usage

```tsx
import MUINumberInput from '@nish1896/mui-components/mui/number-input';
```

```tsx
const [age, setAge] = useState<number | null>(null);

<MUINumberInput
  fieldName="age"
  value={age}
  onValueChange={({ newValue }) => setAge(newValue)}
  onlyIntegers
  nonNegative
/>
```

## Props

`MUINumberInputProps` also accepts the remaining [TextFieldProps](https://v7.mui.com/material-ui/api/text-field/).
Props marked with <strong style={{ color: 'var(--mui-palette-error-main)' }}>{'*'}</strong> are required.

<PropsTable rows={ComponentProps.MUINumberInput} />
