import { DocsPage, PropsTable } from '@/components/docs';
import { componentMetadata } from '@/constants';
import { ComponentProps } from '@/constants/props-table';

export const metadata = componentMetadata.MUICheckbox;

export default function Layout({ children }) {
  return <DocsPage>{children}</DocsPage>;
}

# MUICheckbox

`MUICheckbox` is a single [Checkbox](https://v7.mui.com/material-ui/react-checkbox/)
with a boolean value, an inline label, and optional helper text or error state.

## Usage

```tsx
import MUICheckbox from '@nish1896/mui-components/mui/checkbox';
```

```tsx
const [accepted, setAccepted] = useState(false);

<MUICheckbox
  fieldName="terms"
  value={accepted}
  onValueChange={({ newValue }) => setAccepted(newValue)}
  label="I accept the terms and conditions"
/>
```

## Props

`MUICheckboxProps` also accepts most [CheckboxProps](https://v7.mui.com/material-ui/api/checkbox/).
Props marked with <strong style={{ color: 'var(--mui-palette-error-main)' }}>{'*'}</strong> are required.

<PropsTable rows={ComponentProps.MUICheckbox} />
