import { DocsPage, PropsTable } from '@/components/docs';
import { componentMetadata, componentProps } from '@/constants';
import RatingForm from '@/forms/mui/rating/Client';

export const metadata = componentMetadata.MUIRating;

export default function Layout({ children }) {
  return <DocsPage>{children}</DocsPage>;
}

# MUIRating

`MUIRating` wraps the Material UI [Rating](https://v7.mui.com/material-ui/react-rating/) star input.
The value is a number (or `null` when cleared).

## Playground

Driven by plain React state — pass-through `RatingProps` (`precision`, `max`,
`size`), a label above the control, and required validation via `errorMessage`.

<RatingForm />

## Props

`MUIRatingProps` also accepts most [RatingProps](https://v7.mui.com/material-ui/api/rating/).
Props marked with <strong style={{ color: 'var(--mui-palette-error-main)' }}>{'*'}</strong> are required.

<PropsTable rows={componentProps.MUIRating} />

## Reference
- [MUI Rating](https://v7.mui.com/material-ui/react-rating/)